# VCS and CI
.git
.gitignore
.gitattributes
.github

# Secrets (never bake into an image)
.env
.env.*

# Local virtualenvs and caches
.venv
venv
**/__pycache__
**/*.py[cod]
.pytest_cache
.ruff_cache
.coverage
htmlcov

# Runtime data (lives in named volumes)
media
staticfiles
db.sqlite3
*.sqlite3

# Docs and compose tooling (not needed inside the app image)
docs
site
mkdocs.yml
nginx
scripts
docker-compose*.yml
*.md
LICENSE
.idea
.vscode
